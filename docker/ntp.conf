# Drift file for storing local clock frequency offset
driftfile /var/lib/ntp/ntp.drift

# Leap seconds file
leapfile /usr/share/zoneinfo/leap-seconds.list

# Enable this if you want statistics to be logged.
statsdir /var/log/ntpstats/

statistics loopstats peerstats clockstats
filegen loopstats file loopstats type day enable
filegen peerstats file peerstats type day enable
filegen clockstats file clockstats type day enable

# Use NTP servers 
server 0.pool.ntp.org iburst
server time-a-g.nist.gov iburst
server time-b-g.nist.gov iburst
server ubuntu.pool.ntp.org iburst
server time.google.com iburst

# Preferred server
server 0.pool.ntp.org prefer 

# Use Ubuntu's ntp server as a fallback.
pool ntp.ubuntu.com

# By default, exchange time with everybody, but don't allow configuration.
restrict -4 default kod notrap nomodify nopeer noquery limited
restrict -6 default kod notrap nomodify nopeer noquery limited

# Local users may interrogate the ntp server more closely.
restrict default nomodify notrap noquery 
restrict 127.0.0.1 
restrict ::1
restrict *.quantumworkspace.dev nomodify notrap nopeer noquery 
