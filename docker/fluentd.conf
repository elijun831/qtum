<source>
  @type tail
  path /var/log/syslog
  pos_file /var/log/td-agent/syslog.pos
  tag syslog
</source>

<match syslog>
  @type elasticsearch
  host *.quantumworkspace.dev
  port 9200
  logstash_format true 
  index_name fluentd-syslog
</match>

<match syslog>
  @type stdout
</match>
